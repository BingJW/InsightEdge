# 数据仪表盘指标说明文档

## 概述

数据仪表盘用于监控和评估上市公司市值管理与IP打造项目的效果。通过六大维度指标，全面衡量项目在媒体曝光、社交互动、投资者关系、市值表现等方面的成效。

---

## 一、KPI指标卡片说明

### 1. 市值增长率 (Market Cap Growth Rate)

**当前显示值：902.28%**

#### 📊 定义
市值增长率衡量公司市值在统计周期内的变化幅度，是市值管理成效的核心硬指标。

#### 🔢 计算公式
```
市值增长率 = (期末市值 - 期初市值) / 期初市值 × 100%
```

#### 💻 代码实现位置
```54:115:src/data_analyzer.py
def _calculate_market_cap_kpis(self):
    # 查找市值相关列
    market_cap_cols = [col for col in df.columns if '市值' in col or 'market_cap' in col.lower()]
    
    # 按时间排序计算期初和期末值
    if date_cols:
        df_sorted = df_valid.sort_values('_date')
        initial_cap = df_sorted[market_cap_col].iloc[0]  # 期初市值
        final_cap = df_sorted[market_cap_col].iloc[-1]   # 期末市值
        if pd.notna(initial_cap) and pd.notna(final_cap) and initial_cap > 0:
            kpis['market_cap_growth_rate'] = (final_cap - initial_cap) / initial_cap * 100
```

#### 🎯 为什么计算这个指标？
1. **核心目标衡量**：市值增长是市值管理的最终目标，直接反映资本市场对公司价值的认可
2. **投资回报评估**：帮助评估市值管理项目的ROI（投资回报率）
3. **市场表现对比**：可与大盘指数、行业指数增长率对比，判断是否跑赢市场
4. **趋势监控**：跟踪市值变化趋势，及时发现异常波动

#### 📈 评价标准
- **优秀**：增长率远超行业平均水平，且持续增长
- **良好**：增长率高于行业平均水平
- **需关注**：增长率低于行业平均水平或出现负增长

---

### 2. 总发稿量 (Total Articles)

**当前显示值：600**

#### 📊 定义
统计周期内，关于上市公司及其实控人的正面、中性报道的总数量。

#### 🔢 计算公式
```
总发稿量 = 数据表中的文章/发稿记录总数
```
如果没有找到"发稿"列，则使用数据行数作为总发稿量。

#### 💻 代码实现位置
```173:218:src/data_analyzer.py
def _calculate_media_exposure_kpis(self):
    # 查找发稿相关列
    article_cols = [col for col in df.columns if '发稿' in col or '文章' in col or 'article' in col.lower()]
    
    # 总发稿量 - 优先使用"转发量"工作表的数据
    if self.data_loader:
        forwarding_df = self.data_loader.get_sheet('media_exposure', '转发量')
        if forwarding_df is not None and len(forwarding_df) > 0:
            kpis['total_articles'] = len(forwarding_df)  # 使用转发量工作表行数
    else:
        # 如果没有多工作表支持，使用当前工作表
        if article_cols:
            kpis['total_articles'] = len(df) if df[article_col].dtype == 'object' else int(df[article_col].sum())
        else:
            kpis['total_articles'] = len(df)  # 使用数据行数
```

#### 🎯 为什么计算这个指标？
1. **媒体曝光广度**：衡量公司在媒体上的曝光频率和广度
2. **品牌知名度**：发稿量越多，说明公司品牌和实控人IP的知名度越高
3. **传播效果评估**：评估媒体关系建设和内容策划的效果
4. **行业对比**：与行业平均水平对比，判断传播效果

#### 📈 评价标准
- **优秀**：发稿量远超同期行业平均水平，且保持持续增长趋势
- **良好**：发稿量稳定高于行业平均水平
- **及格**：发稿量与行业平均水平相当
- **需改进**：发稿量低于行业平均水平或出现下滑

---

### 3. 社交互动率 (Social Interaction Rate / Engagement Rate)

**当前显示值：0.00%**

#### 📊 定义
衡量内容吸引粉丝参与互动的效率，是比互动总量更重要的核心指标。反映社交媒体内容的吸引力和粉丝活跃度。

#### 🔢 计算公式
```
社交互动率 = (总互动数 / 总粉丝数) × 100%

其中：
总互动数 = 点赞数 + 评论数 + 转发数 + 其他互动
```

如果没有粉丝数，则使用发帖数计算：
```
社交互动率 = (总互动数 / 总发帖数) × 100%
```

#### 💻 代码实现位置
```250:389:src/data_analyzer.py
def _calculate_social_media_kpis(self):
    # 计算互动总量
    total_engagement = 0
    if like_cols:
        total_engagement += df[like_col].sum()  # 点赞数
    if comment_cols:
        total_engagement += df[comment_col].sum()  # 评论数
    if share_cols:
        total_engagement += df[share_col].sum()  # 转发数
    
    # 计算互动率
    if total_engagement > 0:
        if follower_cols:
            total_followers = df[follower_col].sum()
            if total_followers > 0:
                kpis['engagement_rate'] = (total_engagement / total_followers) * 100
        elif post_cols:
            # 如果没有粉丝数，使用发帖数计算
            total_posts = df[post_col].sum()
            if total_posts > 0:
                kpis['engagement_rate'] = (total_engagement / total_posts) * 100
```

#### 🎯 为什么计算这个指标？
1. **内容质量评估**：互动率反映内容是否真正吸引目标受众
2. **粉丝活跃度**：衡量粉丝的参与度和忠诚度
3. **传播效果**：高互动率说明内容有传播价值，容易被分享
4. **策略优化**：通过互动率数据优化内容策略和发布时间

#### 📈 评价标准（行业参考）
- **微博**：0.5%-1% 为不错，>1% 为优秀
- **抖音**：3%-6% 为不错，>6% 为优秀
- **微信**：关注平均阅读率、在看率、留言率
- **需与自身历史数据对比**：持续提升即为良好

#### ⚠️ 当前显示0.00%的可能原因
1. 数据文件中没有找到"粉丝"、"点赞"、"评论"、"转发"等相关列
2. 数据值全为0或空
3. 数据文件未正确加载

---

### 4. 研报覆盖度 (Research Report Coverage)

**当前显示值：0**

#### 📊 定义
跟踪该公司的券商及分析师数量，以及定期发布研报的频率。反映专业投资机构对公司的关注度。

#### 🔢 计算公式
```
研报覆盖度 = 唯一券商/分析师数量

如果研报列为文本类型：
研报覆盖度 = 研报列的唯一值数量（nunique）

如果研报列为数值类型：
研报覆盖度 = 非零值的数量
```

#### 💻 代码实现位置
```391:468:src/data_analyzer.py
def _calculate_investor_relations_kpis(self):
    # 查找研报相关列 - 扩展匹配模式
    report_cols = [col for col in df.columns if any(keyword in str(col).lower() for keyword in [
        '研报', 'report', '研究报告', '券商研报', 
        '券商数量', '券商', 'broker', 'broker_count', '券商数'
    ])]
    
    if report_cols:
        report_col = report_cols[0]
        if df[report_col].dtype == 'object':
            kpis['research_report_coverage'] = df[report_col].nunique()  # 唯一券商数量
        elif df[report_col].dtype in ['int64', 'float64']:
            if '数量' in str(report_col) or 'count' in str(report_col).lower():
                kpis['research_report_coverage'] = int(df[report_col].sum())  # 券商数量总和
            else:
                kpis['research_report_coverage'] = int((df[report_col] > 0).sum())  # 有覆盖的公司数
    else:
        # 尝试查找分析师/券商列
        analyst_cols = [col for col in df.columns if any(keyword in str(col).lower() for keyword in [
            '分析师', 'analyst', '分析师数量', 'analyst_count'
        ])]
        if analyst_cols:
            analyst_col = analyst_cols[0]
            if df[analyst_col].dtype in ['int64', 'float64']:
                kpis['research_report_coverage'] = int(df[analyst_col].sum())
            else:
                kpis['research_report_coverage'] = int(df[analyst_col].nunique())
        else:
            kpis['research_report_coverage'] = 0
```

#### 🎯 为什么计算这个指标？
1. **专业认可度**：覆盖券商数量增加，表明投资价值受到专业机构更多关注
2. **市场关注度**：研报覆盖度越高，说明公司越受资本市场关注
3. **投资价值评估**：专业机构的关注是公司投资价值的重要信号
4. **投资者关系效果**：衡量投资者关系管理工作的成效

#### 📈 评价标准
- **优秀**：覆盖券商数量持续增加，研报发布频率提高
- **良好**：有一定数量的券商覆盖，定期发布研报
- **需改进**：覆盖券商数量少或没有研报

#### ⚠️ 当前显示0的可能原因
1. 数据文件中没有找到"研报"、"分析师"、"券商"等相关列
2. 数据值全为0或空
3. 数据文件未正确加载

---

## 二、图表说明

### 1. 市值趋势图 (Market Cap Trend)

#### 📊 图表类型
折线图（带垂直柱状图）

#### 📈 显示内容
- **X轴**：时间（日期）或数据点序号
- **Y轴**：总市值（万元），范围从10.0M到90.0M
- **数据线**：蓝色折线，显示市值随时间的变化趋势

#### 🎯 为什么显示这个图表？
1. **趋势可视化**：直观展示市值的变化趋势和波动情况
2. **异常识别**：快速识别市值异常波动的时间点
3. **周期性分析**：观察市值是否存在周期性规律
4. **决策支持**：为市值管理策略调整提供数据依据

#### 💻 代码实现位置
```132:180:src/visualizer.py
def create_market_cap_charts(self):
    # 查找市值列和日期列
    # 创建折线图
    fig = go.Figure()
    fig.add_trace(go.Scatter(
        x=dates,
        y=values,
        mode='lines+markers',
        name='总市值',
        line=dict(color='rgb(102, 126, 234)', width=2)
    ))
```

---

### 2. KPI分布图 (KPI Distribution)

#### 📊 图表类型
环形图（Donut Chart）

#### 📈 显示内容
- **market_cap**（浅蓝色/淡紫色）：市值相关指标占比
- **media_exposure**（紫色）：媒体曝光度相关指标占比

#### 🔢 计算方法
```288:330:templates/dashboard.html
function updateKPIChart(kpiData) {
    // 按类别分组
    const categories = {};
    kpiData.forEach(item => {
        if (!categories[item.category]) {
            categories[item.category] = 0;
        }
        if (typeof item.kpi_value === 'number' && !isNaN(item.kpi_value)) {
            const absValue = Math.abs(item.kpi_value);
            // 如果数值很大，使用对数，否则直接累加
            if (absValue > 1000) {
                categories[item.category] += Math.log10(absValue + 1);
            } else {
                categories[item.category] += absValue;
            }
        }
    });
}
```

#### 🎯 为什么显示这个图表？
1. **指标权重可视化**：展示不同维度指标在整体KPI中的权重分布
2. **平衡性评估**：检查各维度指标是否均衡发展
3. **重点识别**：识别哪些维度是当前的重点关注领域
4. **策略调整**：为资源分配和策略调整提供参考

---

## 三、指标体系框架

系统基于六大维度进行分析：

1. **媒体曝光度指标** (Media Exposure Metrics)
   - 衡量IP打造和市值管理活动在媒体上的关注广度、深度和质量

2. **社交媒体互动数据指标** (Social Media Engagement Metrics)
   - 衡量实控人IP及公司品牌在社交媒体上的影响力、粉丝活跃度

3. **投资者关系指标** (Investor Relations Metrics)
   - 衡量市值管理活动在投资者群体中的影响和效果

4. **市值与财务表现指标** (Market Capitalization & Financial Performance Metrics)
   - 衡量市值管理最终成效的核心硬指标

5. **业务与资源整合实质指标** (Business & Resource Integration Metrics)
   - 衡量市值管理是否反哺主营业务

6. **风险与声誉管控指标** (Risk & Reputation Management Metrics)
   - 衡量风险控制和声誉管理效果

---

## 四、数据来源

### 市值增长率
- **数据来源**：行情软件、市值数据表
- **更新频率**：每日收盘监控、周报、月报

### 总发稿量
- **数据来源**：媒体监测平台（如Cision, Meltwater, 慧科资讯）、搜索引擎收录、合作媒体后台
- **更新频率**：周报、月报、项目周期报告

### 社交互动率
- **数据来源**：各平台后台（微信、微博、抖音、雪球、LinkedIn等）、社交媒体管理工具
- **更新频率**：日报、周报、月报

### 研报覆盖度
- **数据来源**：Wind、同花顺iFinD、券商研究所
- **更新频率**：季度报、半年报、年报

---

## 五、常见问题

### Q1: 为什么社交互动率显示0.00%？
**可能原因**：
1. 数据文件中没有找到"粉丝"、"点赞"、"评论"、"转发"等相关列
2. 数据值全为0或空
3. 数据文件未正确加载

**解决方法**：
- 检查数据文件是否包含社交媒体互动数据
- 确认列名是否匹配（支持：粉丝、follower、点赞、like、评论、comment等）
- 查看系统日志了解具体原因

### Q2: 为什么研报覆盖度显示0？
**可能原因**：
1. 数据文件中没有找到"研报"、"分析师"、"券商"等相关列
2. 数据值全为0或空
3. 数据文件未正确加载

**解决方法**：
- 检查数据文件是否包含投资者关系数据
- 确认列名是否匹配（支持：研报、report、分析师、analyst、券商等）
- 查看系统日志了解具体原因

### Q3: 市值增长率902.28%是否正常？
**说明**：
- 这是一个非常高的增长率，可能表示：
  1. 期初市值较小，期末市值大幅增长
  2. 数据时间跨度较长
  3. 公司确实实现了显著的市值增长

**建议**：
- 对比同期大盘指数和行业指数增长率
- 检查数据的时间范围
- 分析市值增长的原因（业务增长、市场情绪、政策利好等）

---

## 六、使用建议

1. **定期监控**：建议每周或每月查看仪表盘，跟踪指标变化趋势
2. **对比分析**：将当前指标与历史数据、行业平均水平对比
3. **异常关注**：关注异常波动的指标，及时分析原因
4. **策略调整**：根据指标数据调整市值管理和IP打造策略
5. **数据质量**：确保数据文件的完整性和准确性

---

**文档版本**：1.0  
**最后更新**：2026-01-06  
**维护者**：InsightEdge开发团队

