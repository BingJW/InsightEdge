# 商业情报分析系统 - 详细使用说明文档

## 目录

1. [系统概述](#系统概述)
2. [环境要求](#环境要求)
3. [安装配置](#安装配置)
4. [启动系统](#启动系统)
5. [Web界面使用指南](#web界面使用指南)
6. [功能模块详解](#功能模块详解)
7. [API接口文档](#api接口文档)
8. [数据管理](#数据管理)
9. [报告生成](#报告生成)
10. [常见问题](#常见问题)
11. [故障排除](#故障排除)
12. [系统维护](#系统维护)

---

## 系统概述

### 系统简介

商业情报分析系统是一套基于多维度评价指标的商业情报分析工具，为上市公司提供数据采集、清洗、分析、可视化和报告生成的一站式解决方案。

### 核心功能

- **数据采集与导入**：支持Excel数据文件导入，预留数据爬取接口
- **数据清洗与标准化**：自动处理缺失值、异常值、重复数据
- **多维度数据分析**：市值、财务、媒体曝光、社交媒体互动、投资者关系、风险管控
- **可视化仪表盘**：实时展示KPI指标和趋势图表
- **报告生成**：支持日报、周报、季报、年报自动生成
- **用户权限管理**：支持多用户登录和权限控制

### 技术架构

- **后端框架**：Flask (Python)
- **前端技术**：Bootstrap 5 + Chart.js + JavaScript
- **数据处理**：Pandas + NumPy + SciPy
- **可视化**：Chart.js + Plotly
- **报告生成**：ReportLab (PDF) + XlsxWriter (Excel)

---

## 环境要求

### 系统要求

- **操作系统**：Windows 10/11, Linux, macOS
- **Python版本**：Python 3.9 或更高版本
- **内存**：建议 4GB 以上
- **磁盘空间**：至少 2GB 可用空间

### 依赖软件

- Python 3.9+
- pip (Python包管理器)
- Git (可选，用于版本控制)

---

## 安装配置

### 1. 检查Python环境

```bash
python --version
# 应显示 Python 3.9.x 或更高版本
```

### 2. 克隆或下载项目

如果使用Git：
```bash
git clone <repository-url>
cd InsightEdge
```

或直接解压项目文件到目标目录。

### 3. 创建虚拟环境

**Windows:**
```bash
python -m venv venv
```

**Linux/macOS:**
```bash
python3 -m venv venv
```

### 4. 激活虚拟环境

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/macOS:**
```bash
source venv/bin/activate
```

### 5. 安装依赖包

```bash
pip install -r requirements.txt
```

如果下载速度慢，可以使用国内镜像：
```bash
pip install -r requirements.txt -i https://mirrors.ustc.edu.cn/pypi/web/simple
```

### 6. 准备数据文件

确保以下Excel文件位于 `data/` 目录下：

- `第2章 市值与财务表现指标数据.xlsx`
- `第3章 媒体曝光度指标数据.xlsx`
- `第4章 社交媒体互动指标数据 .xlsx`
- `第5章 投资者关系指标数据.xlsx`
- `第6章 风险与声誉管控指标数据.xlsx`

### 7. 配置检查

检查 `config.py` 文件中的配置是否正确：

- 数据文件路径
- 输出目录路径
- 数据库配置（如需要）

---

## 启动系统

### 方法一：使用批处理脚本（Windows推荐）

**命令行模式：**
```bash
.\run_web.bat
```

**PowerShell模式：**
```powershell
.\run_web.bat
# 或
cmd /c run_web.bat
```

### 方法二：直接运行Python脚本

```bash
# 激活虚拟环境后
python app.py
```

### 方法三：命令行模式（命令行分析）

```bash
# 激活虚拟环境后
python main.py
```

### 启动成功标志

看到以下输出表示启动成功：

```
 * Serving Flask app 'app'
 * Debug mode: on
 * Running on http://127.0.0.1:5000
 * Running on http://0.0.0.0:5000
```

### 访问系统

在浏览器中打开：
- **本地访问**：http://localhost:5000
- **局域网访问**：http://<服务器IP>:5000

---

## Web界面使用指南

### 登录系统

1. 打开浏览器访问系统地址
2. 输入用户名和密码
3. 点击"登录"按钮

**默认账户：**

| 用户名 | 密码 | 角色 | 权限 |
|--------|------|------|------|
| admin | admin123 | 管理员 | 全部功能 |
| user1 | user123 | 普通用户 | 查看和报告生成 |

### 界面导航

系统顶部导航栏包含以下模块：

- **仪表盘**：数据概览和KPI展示
- **数据管理**：数据源管理和爬取配置
- **报告中心**：报告生成和下载
- **设置**：用户和系统配置

---

## 功能模块详解

### 1. 仪表盘（Dashboard）

#### 功能概述

仪表盘是系统的核心展示页面，提供关键指标的可视化展示和数据分析。

#### 主要组件

**KPI指标卡片：**
- 市值增长率：显示公司市值的变化趋势
- 总发稿量：媒体曝光度统计
- 社交互动率：社交媒体互动效果
- 研报覆盖度：投资者关系指标

**趋势图表：**
- 市值趋势图：展示市值随时间的变化
- KPI分布图：各指标类别的分布情况

**数据概览表：**
- 数据表名称
- 数据行数和列数
- 缺失值统计
- 重复行统计
- 内存使用情况

#### 操作说明

1. **查看KPI指标**：页面加载后自动显示
2. **刷新数据**：点击"刷新数据"按钮更新最新数据
3. **查看趋势**：鼠标悬停在图表上查看详细数值
4. **数据自动刷新**：系统每5分钟自动刷新一次

### 2. 数据管理（Data Management）

#### 功能概述

数据管理模块用于监控数据源状态、管理数据爬取任务（预留功能）和查看数据表详情。

#### 主要功能

**数据源状态：**
- 显示各数据表的加载状态
- 标识数据质量（正常/有缺失值）
- 实时监控数据更新

**数据爬取状态：**
- 显示爬取任务状态（当前为预留功能）
- 显示最后运行时间
- 显示下次运行计划

**数据表详情：**
- 数据表列表
- 数据状态标识
- 数据统计信息
- 快速操作按钮

#### 操作说明

1. **刷新所有数据**：
   - 点击"刷新所有数据"按钮
   - 确认操作后系统重新加载所有数据文件
   - 处理完成后显示成功提示

2. **数据爬取配置**（预留功能）：
   - 点击"数据爬取"按钮
   - 选择数据源类型（政策、专利、产业动态等）
   - 设置爬取频率（每日/每周/每月）
   - 点击"启动爬取"（当前功能待实现）

3. **查看数据表**：
   - 点击"查看"按钮查看数据表详情
   - 显示数据预览和统计信息

### 3. 报告中心（Reports）

#### 功能概述

报告中心提供各类分析报告的生成、管理和下载功能。

#### 报告类型

**日报（Daily Report）：**
- 每日关键指标快报
- 包含当日重要KPI
- 快速趋势分析
- 格式：PDF

**周报（Weekly Report）：**
- 每周综合分析报告
- 包含一周数据汇总
- 趋势对比分析
- 格式：PDF

**季报（Quarterly Report）：**
- 季度全面绩效报告
- 包含季度数据统计
- 深度趋势分析
- 格式：PDF/Excel

**年报（Yearly Report）：**
- 年度总结报告
- 包含全年数据统计
- 年度趋势回顾
- 格式：PDF/Excel

#### 操作说明

**生成报告：**

1. 点击"生成新报告"按钮
2. 选择报告类型（日报/周报/季报/年报）
3. 点击"生成报告"按钮
4. 等待生成完成（可能需要几分钟）
5. 生成成功后报告出现在报告列表中

**下载报告：**

1. 在报告列表中找到目标报告
2. 点击"下载"按钮
3. 报告文件将下载到本地

**查看报告列表：**

- 报告列表按创建时间倒序排列
- 显示报告文件名、类型、大小、创建时间
- 支持PDF和Excel格式

### 4. 设置（Settings）

#### 功能概述

设置页面用于查看用户信息、系统信息和配置数据爬取参数。

#### 主要功能

**用户信息：**
- 显示当前登录用户名
- 显示用户角色（管理员/普通用户）
- 用户信息为只读

**系统信息：**
- 数据最后更新时间
- 系统版本信息
- 系统状态

**数据爬取配置**（预留功能）：
- 爬取频率设置
- 数据源选择
- 配置保存（当前功能待实现）

---

## API接口文档

### 基础信息

- **Base URL**：`http://localhost:5000`
- **认证方式**：Session-based（需要登录）
- **数据格式**：JSON

### 接口列表

#### 1. 获取KPI摘要

**接口地址：** `GET /api/kpi_summary`

**请求参数：** 无

**响应示例：**
```json
{
  "success": true,
  "data": [
    {
      "category": "market_cap",
      "kpi_name": "market_cap_growth_rate",
      "kpi_value": 15.5
    }
  ],
  "last_update": "2026-01-06T10:30:00"
}
```

#### 2. 获取数据概览

**接口地址：** `GET /api/data_overview`

**请求参数：** 无

**响应示例：**
```json
{
  "success": true,
  "data": [
    {
      "table_name": "market_cap",
      "rows": 19968,
      "columns": 15,
      "missing_values": 0,
      "duplicate_rows": 0,
      "memory_mb": 2.5
    }
  ]
}
```

#### 3. 获取市值趋势

**接口地址：** `GET /api/market_cap_trend`

**请求参数：** 无

**响应示例：**
```json
{
  "success": true,
  "data": {
    "dates": ["2026-01-01", "2026-01-02", "2026-01-03"],
    "values": [1000000, 1050000, 1100000],
    "label": "市值"
  }
}
```

#### 4. 获取报告列表

**接口地址：** `GET /api/reports`

**请求参数：** 无

**响应示例：**
```json
{
  "success": true,
  "data": [
    {
      "filename": "日报_2026-01-06.pdf",
      "type": "pdf",
      "size": 102400,
      "created": "2026-01-06T10:00:00"
    }
  ]
}
```

#### 5. 生成报告

**接口地址：** `POST /api/generate_report`

**请求参数：**
```json
{
  "report_type": "quarterly"
}
```

**报告类型：** `daily`, `weekly`, `quarterly`, `yearly`

**响应示例：**
```json
{
  "success": true,
  "message": "报告生成成功",
  "filename": "季报_2026-01-06.pdf"
}
```

#### 6. 刷新数据

**接口地址：** `POST /api/refresh_data`

**请求参数：** 无

**响应示例：**
```json
{
  "success": true,
  "message": "数据刷新成功",
  "last_update": "2026-01-06T10:30:00"
}
```

#### 7. 下载报告

**接口地址：** `GET /api/reports/<filename>`

**请求参数：** filename（报告文件名）

**响应：** 文件下载

#### 8. 获取爬虫状态（预留）

**接口地址：** `GET /api/crawler_status`

**请求参数：** 无

**响应示例：**
```json
{
  "success": true,
  "data": {
    "status": "未启用",
    "last_run": null,
    "next_run": null,
    "message": "数据爬取功能待实现"
  }
}
```

---

## 数据管理

### 数据文件结构

系统支持以下数据表：

1. **市值与财务表现指标数据** (`market_cap`)
   - 包含市值、财务指标等数据
   - 用于计算市值相关KPI

2. **媒体曝光度指标数据** (`media_exposure`)
   - 包含媒体报道、曝光度等数据
   - 用于计算媒体影响力指标

3. **社交媒体互动指标数据** (`social_media`)
   - 包含社交媒体互动数据
   - 用于计算社交影响力指标

4. **投资者关系指标数据** (`investor_relations`)
   - 包含投资者关系数据
   - 用于计算投资者关系指标

5. **风险与声誉管控指标数据** (`risk_reputation`)
   - 包含风险和声誉数据
   - 用于计算风险管控指标

### 数据清洗流程

系统自动执行以下数据清洗步骤：

1. **去除重复值**：识别并删除重复行
2. **标准化列名**：统一列名格式
3. **处理缺失值**：
   - 数值型：使用中位数填充
   - 分类型：使用众数填充
   - 日期型：删除缺失行
4. **处理异常值**：使用IQR方法识别并处理异常值
5. **格式标准化**：统一日期格式、数据类型

### 数据更新

**手动更新：**
- 在数据管理页面点击"刷新所有数据"
- 或在仪表盘点击"刷新数据"按钮

**自动更新：**
- 系统在首次访问时自动加载数据
- 数据缓存机制提高访问速度

---

## 报告生成

### 报告内容

**日报包含：**
- 当日关键KPI指标
- 最新趋势分析
- 风险提示

**周报包含：**
- 本周KPI汇总
- 周度趋势分析
- 竞品对比（如有）

**季报包含：**
- 季度KPI全面统计
- 深度趋势分析
- 关键发现与建议
- 详细数据附录

**年报包含：**
- 年度绩效总结
- 年度趋势回顾
- 战略复盘与规划
- 完整数据与ESG表现

### 报告格式

- **PDF格式**：适用于打印和正式报告
- **Excel格式**：适用于数据分析和进一步处理

### 报告存储

所有生成的报告存储在：
```
output/reports/
```

---

## 常见问题

### Q1: 无法启动Web服务

**可能原因：**
- 端口5000被占用
- 虚拟环境未激活
- 依赖包未安装完整

**解决方法：**
1. 检查端口占用：`netstat -ano | findstr :5000`
2. 激活虚拟环境：`venv\Scripts\activate`
3. 重新安装依赖：`pip install -r requirements.txt`

### Q2: 登录后页面空白

**可能原因：**
- 数据文件未加载
- JavaScript错误
- 浏览器兼容性问题

**解决方法：**
1. 检查浏览器控制台错误信息
2. 尝试刷新页面
3. 清除浏览器缓存
4. 使用Chrome或Edge浏览器

### Q3: 数据加载失败

**可能原因：**
- Excel文件路径错误
- 文件格式不正确
- 文件损坏

**解决方法：**
1. 检查`data/`目录下文件是否存在
2. 确认文件名与配置一致
3. 检查Excel文件是否可以正常打开

### Q4: 报告生成失败

**可能原因：**
- 数据未加载完成
- 磁盘空间不足
- 权限问题

**解决方法：**
1. 等待数据加载完成后再生成报告
2. 检查磁盘空间
3. 检查输出目录权限

### Q5: 图表不显示

**可能原因：**
- Chart.js库加载失败
- 数据格式不正确
- 网络问题

**解决方法：**
1. 检查网络连接
2. 刷新页面
3. 检查浏览器控制台错误

---

## 故障排除

### 查看日志

系统日志文件位于：
```
output/system.log
```

### 常见错误及解决

**错误1：ModuleNotFoundError**
```
解决方法：pip install <缺失的模块名>
```

**错误2：Port already in use**
```
解决方法：
1. 关闭占用端口的程序
2. 或修改app.py中的端口号
```

**错误3：Permission denied**
```
解决方法：
1. 以管理员身份运行
2. 检查文件/目录权限
```

**错误4：Database connection error**
```
解决方法：
1. 检查数据库配置
2. 确认数据库服务运行
3. 或暂时禁用数据库功能
```

### 重置系统

如需重置系统：

1. 清除数据缓存：
   ```python
   # 删除 output/ 目录下的缓存文件
   ```

2. 重新加载数据：
   - 在Web界面点击"刷新所有数据"
   - 或重启服务

3. 清除报告：
   - 删除 `output/reports/` 目录下的文件

---

## 系统维护

### 定期维护任务

1. **数据备份**
   - 定期备份 `data/` 目录
   - 备份 `output/` 目录

2. **日志清理**
   - 定期清理 `output/system.log`
   - 保留最近30天的日志

3. **报告归档**
   - 定期归档旧报告
   - 建议保留最近3个月的报告

4. **依赖更新**
   - 定期更新Python包
   - 检查安全更新

### 性能优化

1. **数据缓存**
   - 系统自动缓存已加载的数据
   - 减少重复加载时间

2. **数据库优化**（如使用）
   - 定期清理历史数据
   - 创建必要的索引

3. **报告生成优化**
   - 避免同时生成多个报告
   - 使用异步任务处理（未来版本）

### 安全建议

1. **修改默认密码**
   - 首次使用后立即修改默认账户密码
   - 使用强密码策略

2. **生产环境部署**
   - 使用WSGI服务器（如Gunicorn）
   - 配置HTTPS
   - 设置防火墙规则

3. **数据安全**
   - 定期备份数据
   - 限制文件访问权限
   - 加密敏感数据

---

## 技术支持

### 获取帮助

如遇到问题，请：

1. 查看本文档的常见问题部分
2. 检查系统日志文件
3. 查看终端错误输出
4. 联系技术支持团队

### 系统版本

当前版本：v1.0.0

### 更新日志

**v1.0.0 (2026-01-06)**
- 初始版本发布
- 实现基础数据加载和清洗
- 实现Web界面和API
- 实现报告生成功能
- 预留数据爬取接口

---

## 附录

### A. 文件结构

```
InsightEdge/
├── app.py                 # Flask Web应用主程序
├── main.py                # 命令行主程序
├── config.py              # 系统配置文件
├── requirements.txt       # Python依赖包列表
├── run_web.bat           # Web服务启动脚本
├── start.bat             # 命令行启动脚本
├── data/                 # 数据文件目录
│   ├── 第2章 市值与财务表现指标数据.xlsx
│   ├── 第3章 媒体曝光度指标数据.xlsx
│   ├── 第4章 社交媒体互动指标数据 .xlsx
│   ├── 第5章 投资者关系指标数据.xlsx
│   ├── 第6章 风险与声誉管控指标数据.xlsx
│   └── 上市公司市值管理与IP打造效果评价指标体系.txt
├── src/                  # 源代码目录
│   ├── data_loader.py    # 数据加载模块
│   ├── data_cleaner.py   # 数据清洗模块
│   ├── data_analyzer.py  # 数据分析模块
│   ├── report_generator.py  # 报告生成模块
│   └── visualizer.py     # 可视化模块
├── templates/            # HTML模板目录
│   ├── base.html
│   ├── login.html
│   ├── dashboard.html
│   ├── data_management.html
│   ├── reports.html
│   └── settings.html
├── static/               # 静态资源目录
│   ├── css/
│   ├── js/
│   └── img/
└── output/               # 输出目录
    ├── cleaned_data/     # 清洗后的数据
    ├── reports/          # 生成的报告
    ├── visualizations/   # 可视化图表
    └── system.log        # 系统日志
```

### B. 快捷键

- **Ctrl+C**：停止Web服务
- **F5**：刷新页面
- **Ctrl+Shift+R**：强制刷新（清除缓存）

### C. 浏览器兼容性

推荐使用以下浏览器：
- Chrome 90+
- Edge 90+
- Firefox 88+
- Safari 14+

---

**文档版本：** 1.0.0  
**最后更新：** 2026-01-06  
**维护者：** InsightEdge开发团队



