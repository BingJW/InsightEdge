# 系统架构分析与改进方案

## 一、实际数据结构分析

### 1. 市值与财务表现指标数据 (market_cap)
**实际列名：**
- `Unnamed: 0` (索引)
- `TS股票代码` (如：600036.SH)
- `股票名称` (如：招商银行)
- `交易日期` (格式：20251031，YYYYMMDD)
- `总市值(万元)` (float64)

**数据特点：**
- 19968行数据，时间序列数据
- 日期格式为YYYYMMDD整数格式
- 需要转换为标准日期格式

**当前问题：**
- ✅ 市值增长率计算已修复（按时间排序）
- ⚠️ 日期列名是"交易日期"，需要识别

---

### 2. 媒体曝光度指标数据 (media_exposure)
**实际列名：**
- `公司名称` (object, 100/600非空)
- `公司代码` (object, 600/600)
- `时间段（开始日期-结束日期）` (datetime64[ns])
- `正面` (float64) - 情感分析：正面占比
- `中性` (float64) - 情感分析：中性占比
- `负面` (float64) - 情感分析：负面占比
- `市值增长率` (float64)
- `PE增长率` (float64)
- `PETTM增长率` (float64)
- `PB增长率` (float64)
- `PS增长率` (float64)
- `PSTTM增长率` (float64)

**数据特点：**
- 600行数据
- **这不是传统的"发稿量"数据，而是情感分析和估值增长率数据**
- 包含正面/中性/负面情感占比
- 包含估值指标增长率

**当前问题：**
- ❌ **总发稿量计算错误**：代码在找"发稿"、"文章"列，但实际数据中没有这些列
- ✅ 应该使用行数（600）作为总发稿量（已修复）
- ⚠️ 可以计算情感分析指标（正面/中性/负面占比）

---

### 3. 社交媒体互动指标数据 (social_media)
**实际列名：**
- `日期范围` (object) - 格式：2025-04-28~2025-05-04
- `公司名称` (object)
- `关键意见用户占比` (int64) - 这是KOL占比，不是互动率

**数据特点：**
- 2600行数据
- **只有"关键意见用户占比"，没有粉丝数、点赞数、评论数、转发数**
- 日期格式是范围格式（开始日期~结束日期）

**当前问题：**
- ❌ **社交互动率无法计算**：缺少必要的粉丝数、互动数数据
- ✅ 可以计算关键意见用户占比的平均值或趋势
- ⚠️ 需要明确：这个数据文件是否包含互动数据，还是只有KOL占比？

---

### 4. 投资者关系指标数据 (investor_relations)
**实际列名：**
- `证券代码` (object)
- `证券简称` (object)
- `券商数量` (float64) ✅ **这个可以用来计算研报覆盖度**
- `分析师数量` (float64)
- `研报发布频率` (float64)
- `机构投资者持股占比` (float64)
- `一致预期目标价` (float64)
- `信息披露质量评分` (int64) - A为100，B为80
- `机构投资者持股比例` (float64)
- `投资者活动参与度` (float64)
- `日均交易额(万元)` (float64)
- `相对行业平均估值溢价` (float64)

**数据特点：**
- 100行数据（100家公司）
- **有"券商数量"列，可以用来计算研报覆盖度**
- 包含丰富的投资者关系指标

**当前问题：**
- ❌ **研报覆盖度计算错误**：代码在找"研报"列，但实际列名是"券商数量"
- ✅ 应该使用"券商数量"列来计算研报覆盖度
- ⚠️ 需要修复列名匹配逻辑

---

### 5. 风险与声誉管控指标数据 (risk_reputation)
**实际列名：**
- `成分股代码Constituent Code` (int64)
- `成分股名称Constituent Name` (object)
- `行业分类` (object)
- `Wind ESG评分` (object)
- `查询负面舆情数量（2024年）` (object)
- `关注度` (int64)
- `监管处罚次数（含问询函等，2024年）` (int64)
- `负面舆情总数` (int64)
- `正面舆情总数` (int64)
- `负面舆情占比` (float64)
- `负面舆情1级数量` (int64)
- `负面舆情2级数量` (int64)
- `负面舆情3级数量` (int64)
- `重大舆情发生次数（影响市值100万）` (int64)
- `舆情平均平息时间（天）（超过30天）` (int64)
- `1-2级占比` (float64)
- `2-3级占比` (float64)
- `平均占比` (float64)
- `最新股价` (float64)
- `总市值(万元)` (float64)

**数据特点：**
- 100行数据
- 包含ESG评分、负面舆情、监管处罚等风险指标

---

## 二、系统架构问题分析

### 问题1：列名匹配不准确
**现状：**
- 数据清洗时会将列名转换为小写并替换空格为下划线
- 但实际列名与代码中查找的关键词不匹配

**示例：**
- 代码查找：`'研报' in col or 'report' in col.lower()`
- 实际列名：`券商数量`（清洗后：`券商数量` → `券商数量`，不会匹配到"研报"）

### 问题2：数据结构理解偏差
**现状：**
- 代码假设数据包含传统意义上的"发稿量"、"粉丝数"、"点赞数"等
- 但实际数据是汇总后的指标数据，不是原始明细数据

**示例：**
- 媒体曝光度数据：不是每篇文章的记录，而是时间段的情感分析汇总
- 社交媒体数据：不是每条帖子的记录，而是时间段的KOL占比汇总

### 问题3：日期格式处理
**现状：**
- 市值数据：日期是YYYYMMDD整数格式（20251031）
- 社交媒体数据：日期是范围格式（2025-04-28~2025-05-04）
- 媒体曝光度数据：日期是datetime格式

---

## 三、系统架构改进方案

### 方案1：改进列名匹配逻辑（推荐）

#### 1.1 市值增长率
**当前代码查找：** `'市值' in col or 'market_cap' in col.lower()`
**实际列名：** `总市值(万元)`
**修复方案：** ✅ 已匹配到

#### 1.2 总发稿量
**当前代码查找：** `'发稿' in col or '文章' in col or 'article' in col.lower()`
**实际列名：** 无此列
**修复方案：** ✅ 已修复（使用行数）

#### 1.3 社交互动率
**当前代码查找：** `'粉丝' in col or 'follower' in col.lower()`
**实际列名：** 无此列（只有"关键意见用户占比"）
**修复方案：** 
- ⚠️ **需要确认数据源**：是否还有其他数据文件包含互动数据？
- 如果没有，应该显示"数据不足"而不是0.00%
- 可以使用"关键意见用户占比"作为替代指标

#### 1.4 研报覆盖度
**当前代码查找：** `'研报' in col or 'report' in col.lower()`
**实际列名：** `券商数量`
**修复方案：** ✅ 需要修复，添加"券商数量"、"券商"等关键词

---

### 方案2：数据映射配置（推荐）

创建一个数据映射配置文件，明确每个KPI对应的实际列名：

```python
# config/kpi_column_mapping.py
KPI_COLUMN_MAPPING = {
    'market_cap': {
        'market_cap_col': ['总市值', '市值', 'market_cap', '总市值(万元)'],
        'date_col': ['交易日期', '日期', 'date', '时间', '时间'],
        'growth_rate': 'market_cap_growth_rate'
    },
    'media_exposure': {
        'total_articles': None,  # 使用行数
        'sentiment_positive': ['正面', 'positive'],
        'sentiment_neutral': ['中性', 'neutral'],
        'sentiment_negative': ['负面', 'negative']
    },
    'social_media': {
        'kol_ratio': ['关键意见用户占比', 'kol', 'koc', '关键意见'],
        'engagement_rate': None,  # 数据不足
        'date_range': ['日期范围', 'date_range', '时间段']
    },
    'investor_relations': {
        'research_report_coverage': ['券商数量', '券商', 'broker', 'analyst'],
        'analyst_count': ['分析师数量', '分析师', 'analyst'],
        'trading_value': ['日均交易额', '交易额', 'trading']
    }
}
```

---

### 方案3：智能列名识别（推荐）

改进列名匹配逻辑，支持更多变体和同义词：

```python
def find_column_by_keywords(df, keyword_groups):
    """
    根据关键词组查找列名
    keyword_groups: [['关键词1', '关键词2'], ['关键词3', '关键词4']]
    返回第一个匹配的列名
    """
    for keywords in keyword_groups:
        for col in df.columns:
            col_lower = str(col).lower()
            if any(keyword.lower() in col_lower for keyword in keywords):
                return col
    return None
```

---

## 四、具体修复建议

### 修复1：研报覆盖度 - 使用"券商数量"列

```python
# 在 _calculate_investor_relations_kpis 中
report_cols = [
    col for col in df.columns 
    if any(keyword in col.lower() for keyword in [
        '研报', 'report', '券商数量', '券商', 'broker', 
        'analyst', '分析师数量'
    ])
]

if report_cols:
    report_col = report_cols[0]
    if df[report_col].dtype in ['int64', 'float64']:
        # 使用券商数量作为研报覆盖度
        kpis['research_report_coverage'] = df[report_col].sum()  # 或 mean()，根据业务需求
```

### 修复2：社交互动率 - 处理数据不足的情况

```python
# 在 _calculate_social_media_kpis 中
# 如果找不到粉丝数和互动数，使用关键意见用户占比作为替代指标
kol_cols = [col for col in df.columns if '关键意见' in col or 'kol' in col.lower()]

if kol_cols:
    kol_col = kol_cols[0]
    if df[kol_col].dtype in ['int64', 'float64']:
        kpis['kol_ratio'] = df[kol_col].mean()  # 或根据业务需求计算
        # 如果确实没有互动数据，可以显示KOL占比作为替代
        if not follower_cols and total_engagement == 0:
            logger.warning("缺少粉丝数和互动数据，无法计算传统互动率")
            # 可以考虑使用KOL占比作为替代指标
```

### 修复3：媒体曝光度 - 计算情感分析指标

```python
# 在 _calculate_media_exposure_kpis 中
# 添加情感分析指标计算
sentiment_positive_cols = [col for col in df.columns if '正面' in col or 'positive' in col.lower()]
sentiment_neutral_cols = [col for col in df.columns if '中性' in col or 'neutral' in col.lower()]
sentiment_negative_cols = [col for col in df.columns if '负面' in col or 'negative' in col.lower()]

if sentiment_positive_cols:
    kpis['sentiment_positive_ratio'] = df[sentiment_positive_cols[0]].mean()
if sentiment_neutral_cols:
    kpis['sentiment_neutral_ratio'] = df[sentiment_neutral_cols[0]].mean()
if sentiment_negative_cols:
    kpis['sentiment_negative_ratio'] = df[sentiment_negative_cols[0]].mean()
```

### 修复4：日期格式处理

```python
# 处理不同格式的日期列
def parse_date_column(df, date_col):
    """解析不同格式的日期列"""
    if df[date_col].dtype == 'int64':
        # YYYYMMDD格式：20251031
        return pd.to_datetime(df[date_col].astype(str), format='%Y%m%d', errors='coerce')
    elif df[date_col].dtype == 'object':
        # 范围格式：2025-04-28~2025-05-04
        # 提取开始日期
        return pd.to_datetime(df[date_col].str.split('~').str[0], errors='coerce')
    else:
        # datetime格式
        return pd.to_datetime(df[date_col], errors='coerce')
```

---

## 五、系统架构建议

### 1. 数据层（Data Layer）
- **数据加载器**：支持多种Excel格式和列名变体
- **数据清洗器**：智能识别日期格式、数据类型
- **数据映射器**：配置化的列名映射，便于维护

### 2. 计算层（Calculation Layer）
- **KPI计算器**：基于配置的列名映射进行计算
- **指标验证器**：检查数据完整性，给出警告
- **替代指标计算器**：当主要指标数据不足时，计算替代指标

### 3. 展示层（Presentation Layer）
- **仪表盘**：清晰标注数据来源和计算方式
- **数据质量提示**：当数据不足时，显示提示信息
- **指标说明**：每个指标显示数据来源和计算方法

---

## 六、立即需要修复的问题

### 优先级1：研报覆盖度
- **问题**：找不到"研报"列
- **解决**：添加"券商数量"、"券商"等关键词匹配
- **影响**：当前显示0，修复后应显示实际券商数量

### 优先级2：社交互动率
- **问题**：缺少粉丝数和互动数数据
- **解决**：
  1. 确认是否有其他数据源
  2. 如果没有，使用"关键意见用户占比"作为替代指标
  3. 在界面上标注"数据不足"或显示替代指标

### 优先级3：日期格式处理
- **问题**：市值数据的日期是YYYYMMDD整数格式
- **解决**：添加日期格式转换逻辑

---

## 七、数据文件结构总结

| 数据表 | 实际用途 | 主要指标 | 数据特点 |
|--------|---------|---------|---------|
| market_cap | 市值时间序列 | 市值增长率 | 19968行，YYYYMMDD日期格式 |
| media_exposure | 情感分析+估值 | 总发稿量(行数)、情感占比 | 600行，时间段汇总数据 |
| social_media | KOL占比 | 关键意见用户占比 | 2600行，只有KOL占比，无互动数据 |
| investor_relations | 投资者关系指标 | 研报覆盖度(券商数量) | 100行，100家公司横截面数据 |
| risk_reputation | 风险与声誉 | ESG、负面舆情等 | 100行，风险指标数据 |

---

## 八、下一步行动

1. ✅ 修复研报覆盖度：添加"券商数量"关键词匹配
2. ✅ 修复社交互动率：处理数据不足的情况，使用KOL占比作为替代
3. ✅ 修复日期格式：处理YYYYMMDD格式
4. ✅ 添加数据质量检查：当数据不足时给出明确提示
5. ✅ 更新文档：说明每个指标的实际数据来源



