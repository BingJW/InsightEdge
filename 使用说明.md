# 商业情报分析系统 - 使用说明

## 快速开始

### 1. 环境安装

#### 方法一：使用Conda（推荐）

```bash
# 创建conda环境
conda env create -f environment.yml

# 激活环境
conda activate insightedge_bi
```

#### 方法二：使用pip

```bash
# 安装依赖
pip install -r requirements.txt
```

#### 方法三：使用安装脚本（Windows）

双击运行 `install.bat` 文件，脚本会自动创建conda环境。

### 2. 准备数据文件

确保以下5个Excel文件已放置在 `data/` 目录下：

- ✅ 第2章 市值与财务表现指标数据.xlsx
- ✅ 第3章 媒体曝光度指标数据.xlsx
- ✅ 第4章 社交媒体互动指标数据 .xlsx
- ✅ 第5章 投资者关系指标数据.xlsx
- ✅ 第6章 风险与声誉管控指标数据.xlsx

### 3. 运行系统

#### 方法一：使用主程序

```bash
python main.py
```

#### 方法二：使用运行脚本（Windows）

双击运行 `run.bat` 文件。

#### 方法三：测试系统

```bash
python test_system.py
```

## 系统功能说明

### 1. 数据加载模块 (`src/data_loader.py`)

**功能：**
- 自动加载5个Excel数据文件
- 分析数据结构、缺失值、异常值
- 生成数据加载摘要报告

**使用方法：**
```python
from src.data_loader import DataLoader

loader = DataLoader()
raw_data = loader.load_all_excel_files()
summary = loader.get_data_summary()
```

### 2. 数据清洗模块 (`src/data_cleaner.py`)

**功能：**
- 去除重复值
- 填补缺失值（数值列用中位数，文本列用众数）
- 处理异常值（使用IQR方法）
- 标准化日期格式
- 统一金额单位
- 优化数据类型以节省内存

**使用方法：**
```python
from src.data_cleaner import DataCleaner

cleaner = DataCleaner()
cleaned_data = cleaner.clean_all_data(raw_data)
cleaner.save_cleaned_data()  # 保存清洗后的数据
```

### 3. 数据分析模块 (`src/data_analyzer.py`)

**功能：**
- 计算市值与财务表现指标（市值增长率、股价涨幅、ROE等）
- 计算媒体曝光度指标（总发稿量、媒体覆盖、AVE等）
- 计算社交媒体互动指标（粉丝数、互动率、病毒传播系数等）
- 计算投资者关系指标（研报覆盖度、交易额、持股比例等）
- 计算风险与声誉管控指标（负面舆情占比、风险事件数等）
- 趋势分析（线性回归分析）

**使用方法：**
```python
from src.data_analyzer import DataAnalyzer

analyzer = DataAnalyzer(cleaned_data)
kpis = analyzer.calculate_all_kpis()
summary = analyzer.get_kpi_summary()
```

### 4. 可视化模块 (`src/visualizer.py`)

**功能：**
- KPI综合仪表盘（仪表盘样式）
- 市值趋势图（折线图）
- 媒体曝光度分析（柱状图、饼图）
- 社交媒体互动分析（柱状图）
- 投资者关系图表（折线图）
- 风险与声誉分析（柱状图）
- 各维度KPI对比（柱状图）

**输出格式：** HTML文件（可在浏览器中打开）

**使用方法：**
```python
from src.visualizer import Visualizer

visualizer = Visualizer(cleaned_data, kpis)
visualizer.create_all_visualizations()
visualizer.create_dashboard_html()  # 创建综合仪表盘
```

### 5. 报告生成模块 (`src/report_generator.py`)

**功能：**
- 生成日报（PDF格式）
- 生成周报（PDF格式）
- 生成季报（PDF格式，包含完整分析）
- 生成年报（PDF格式）
- 生成Excel报告（数据表格格式）

**报告内容：**
- 执行摘要
- 关键指标表格
- 各维度详细分析
- 目标达成对比
- 成功亮点与最佳实践
- 挑战与改进建议
- 下季度行动计划

**使用方法：**
```python
from src.report_generator import ReportGenerator

generator = ReportGenerator(cleaned_data, kpis, trend_analysis)
generator.generate_quarterly_report()  # 生成季报
generator.generate_excel_report('quarterly')  # 生成Excel报告
```

### 6. 数据库存储模块 (`src/database_manager.py`)

**功能：**
- 支持MySQL数据库存储
- 支持MongoDB数据库存储
- 保存清洗后的数据
- 保存KPI计算结果
- 查询数据功能

**配置方法：**

编辑 `config.py` 文件中的数据库配置：

```python
DATABASE_CONFIG = {
    'mysql': {
        'host': 'localhost',
        'port': 3306,
        'user': 'root',
        'password': 'your_password',
        'database': 'insightedge_bi',
        'charset': 'utf8mb4'
    },
    'mongodb': {
        'host': 'localhost',
        'port': 27017,
        'database': 'insightedge_bi'
    }
}
```

**使用方法：**
```python
from src.database_manager import DatabaseManager

# MySQL存储
db_manager = DatabaseManager('mysql')
db_manager.save_data(cleaned_data)
db_manager.save_kpis(kpis)
db_manager.close_connection()

# MongoDB存储
db_manager = DatabaseManager('mongodb')
db_manager.save_data(cleaned_data)
db_manager.save_kpis(kpis)
db_manager.close_connection()
```

## 输出文件说明

系统运行后会在 `output/` 目录下生成以下文件：

### 清洗后的数据 (`output/cleaned_data/`)
- `market_cap_cleaned.xlsx`
- `media_exposure_cleaned.xlsx`
- `social_media_cleaned.xlsx`
- `investor_relations_cleaned.xlsx`
- `risk_reputation_cleaned.xlsx`

### 可视化图表 (`output/visualizations/`)
- `kpi_dashboard.html` - KPI综合仪表盘
- `market_cap_trend.html` - 市值趋势图
- `media_exposure.html` - 媒体曝光度分析
- `social_engagement.html` - 社交媒体互动分析
- `dashboard.html` - 综合仪表盘（包含所有图表）

### 报告文件 (`output/reports/`)
- `日报_YYYY-MM-DD.pdf`
- `周报_YYYY-MM-DD.pdf`
- `季报_YYYY-MM-DD.pdf`
- `年报_YYYY-MM-DD.pdf`
- `quarterly_报告_YYYY-MM-DD.xlsx`

### 日志文件 (`output/`)
- `system.log` - 系统运行日志

## 常见问题

### Q1: 提示"文件不存在"
**A:** 请检查 `data/` 目录下是否包含所有5个Excel文件，文件名必须完全匹配。

### Q2: 数据库连接失败
**A:** 
1. 检查数据库服务是否启动
2. 检查 `config.py` 中的数据库配置是否正确
3. 如果不需要数据库存储，可以在 `main.py` 中将 `save_to_db=False`

### Q3: 生成的图表无法显示中文
**A:** 系统已配置中文字体支持，如果仍有问题，请检查系统是否安装了中文字体。

### Q4: 报告生成失败
**A:** 
1. 检查是否安装了 `reportlab` 库
2. 检查输出目录是否有写入权限
3. 查看日志文件 `output/system.log` 了解详细错误信息

### Q5: 如何自定义报告内容
**A:** 编辑 `src/report_generator.py` 文件中的相应方法，修改报告模板和内容。

## 系统架构

```
数据流程：
Excel文件 → 数据加载 → 数据清洗 → 数据分析 → 可视化/报告生成 → 数据库存储

模块关系：
main.py (主程序)
├── data_loader.py (数据加载)
├── data_cleaner.py (数据清洗)
├── data_analyzer.py (数据分析)
├── visualizer.py (可视化)
├── report_generator.py (报告生成)
└── database_manager.py (数据库存储)
```

## 技术支持

如遇到问题，请：
1. 查看日志文件 `output/system.log`
2. 运行测试脚本 `python test_system.py` 检查各模块
3. 检查数据文件格式是否正确

## 更新日志

### v1.0.0 (2024)
- 初始版本发布
- 实现数据加载、清洗、分析、可视化、报告生成功能
- 支持MySQL和MongoDB数据库存储




